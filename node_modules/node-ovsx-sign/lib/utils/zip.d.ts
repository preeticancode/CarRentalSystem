/// <reference types="node" />
export declare const zipBuffers: (files: {
    filename: string;
    buffer: Buffer;
}[]) => Promise<Buffer>;
/**
 * Extract a specific file from a zip archive as a buffer using streams
 * @param zipFilePath The path to the zip file
 * @param fileName The name of the file to extract (e.g., ".signature.sig")
 * @returns A promise that resolves to a buffer containing the file contents
 */
export declare const extractFileAsBufferUsingStreams: (zipFilePath: string, fileName: string) => Promise<Buffer>;
export declare function readZip(packagePath: string): Promise<Map<string, Buffer>>;
//# sourceMappingURL=zip.d.ts.map